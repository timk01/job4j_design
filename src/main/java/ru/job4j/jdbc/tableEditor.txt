2. Описание методов:

        - createTable() – создает пустую таблицу без столбцов с указанным именем;

        - dropTable() – удаляет таблицу по указанному имени;

        - addColumn() – добавляет столбец в таблицу;

        - dropColumn() – удаляет столбец из таблицы;

        - renameColumn() – переименовывает столбец.

3. Надо создать метод main(), в котором последовательно изменять состояние таблицы,
вызывая написанные методы, и после каждого метода  для контроля выводить ее схему,
а именно ее столбцы и их типы, используя метод getTableScheme()

4.  Чтение настроек должно идти из файла *.properties.

5.  Настройки для подключения будем получать с помощью Properties.

6. Удалить дублирование кода